# Copy this to .env and fill in your values
AISSTREAM_API_KEY=your_key_here

# Optional: maritime news headlines on the Dashboard
# Free dev tier at https://newsapi.org (100 req/day)
# If omitted, falls back to free RSS feeds (no key needed)
NEWS_API_KEY=

# Optional — used only for tests
CACHE_DB_PATH=
